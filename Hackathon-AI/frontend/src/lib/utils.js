// This file is for potential helper functions. For now, it can be empty or
// export a function for combining class names if needed.
export function cn(...inputs) {
  return inputs.filter(Boolean).join(' ');
}